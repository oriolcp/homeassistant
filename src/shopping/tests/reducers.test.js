
describe('Shopping reducers', () => {
  it('should add an item to currentList and allItems if it is new', () => {

  });

  it('should not add an existing item to allItems', () => {

  });

  it('should delete an item from currentItems but keep it in allItems', () => {

  });

  it('should toggle picking', () => {

  });

  it('should reset currentList and copy it to history when checkouting', () => {

  });
});

describe('Shopping selectors', () => {
  it('should get all items', () => {

  });

  it('should get current shopping items', () => {

  });

  it('should get unused items based on current list', () => {

  });

  it('should know if any item has been picked already', () => {

  });
});
