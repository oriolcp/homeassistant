import Shopping from '../shopping';
import {reduxRender, fireEvent} from 'utils/testing';


describe('Shopping', () => {
  beforeEach(() => {

  });

  it('should have a list of shopping items', () => {

  });

  it('should have checkout button if any item is picked', () => {

  });

  it('should show a empty list message if the list is empty', () => {

  });

  it('should dispatch togglePickItem when togglingPickItem', () => {

  });

  it('should dispatch deleteItem when deletingItem', () => {

  });
});
